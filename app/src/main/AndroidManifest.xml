<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sample.rs.storedoor" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.SplashActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.BaseActivity" />
        <activity android:name=".activities.ProductDetailActivity" />
        <activity android:name=".activities.CategoryDetailActivity" />
        <activity
            android:name=".activities.NavigationDrawerBaseActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activities.FABBaseActivity"
            android:label="@string/title_activity_fabbase" >
        </activity>
        <activity
            android:name=".activities.FABNavigationDrawerBaseActivity"
            android:label="@string/title_activity_fabnavigation_drawer_base" >
        </activity>
        <activity
            android:name=".activities.CartActivity"
            android:label="@string/title_activity_cart" >
        </activity>
    </application>

</manifest>
